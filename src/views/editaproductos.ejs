<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edita productos</title>
</head>

<body>

    <div class="container mt-5">
        <div class="row">
          <div class="col-md-5 offset-md-3 card">
            <div class="card-body">
              <form action="/update/<%= data.productos.id %>" method="POST">
                <div class="form-group">
                  <input type="text" name="nombre" value="<%= data.productos.nombre%>"  class="form-control">
                </div>
                <div class="form-group">
                  <input type="text" name="precio" value="<%= data.productos.precio%>"  class="form-control">
                </div>
                <div class="form-group">
                  <input type="text" name="peso" value="<%= data.productos.peso%>"  class="form-control">
                </div>
                <div class="form-group">
                    <p>Categoria</p>
                    <select id="inputState" name="tipo" class="form-control">

                        <% if (data.categoria) { %>
                            <% for(var i = 0; i < data.categoria.length; i++) { %>
                                
                                <option value="<%= data.categoria[i].id %>"><%= data.categoria[i].id%> - <%= data.categoria[i].nombre%></option>
                            <% } %>
                          <% } %>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary">
                  Editar producto
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>

</body>

</html>